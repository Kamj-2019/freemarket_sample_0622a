= render 'layouts/header'
.main__visual
.main__single-column
  .content
    %section
      %h1 ピックアップカテゴリー
      %h2
        = link_to "#{@category1.first_category}新着アイテム", category_item_path(1)
      .contents__items
        - @items1.first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            = image_tag(item.item_images[0].image_url, alt: '商品画像')
            %div
              %div
                = item.item_title
              %span.price 
                ¥
                = item.price
              %span.likes
                100
      .contents__all-items
        = link_to 'すべての商品を見る', ''

      %h2
        = link_to "#{@category2.first_category}新着アイテム", category_item_path(2)
      .contents__items
        - @items2.first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            = image_tag(item.item_images[0].image_url, width: '135', alt: '商品画像')
            %div
              %div
                = item.item_title
              %span.price 
                ¥
                = item.price
              %span.likes
                =fa_icon "heart-o"
                135
      .contents__all-items
        = link_to 'すべての商品を見る', ''

      %h2
        = link_to "#{@category3.first_category}新着アイテム", category_item_path(3)
      .contents__items
        - @items3.first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            = image_tag(item.item_images[0].image_url, width: '135', alt: '商品画像')
            %div
              %div
                = item.item_title
              %span.price 
                ¥
                = item.price
              %span.likes
                =fa_icon "heart-o"
                135
      .contents__all-items
        = link_to 'すべての商品を見る', ''

      %h2
        = link_to "#{@category4.first_category}新着アイテム", category_item_path(7)
      .contents__items
        - @items4.first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            = image_tag(item.item_images[0].image_url, width: '135', alt: '商品画像')
            %div
              %div
                = item.item_title
              %span.price 
                ¥
                = item.price
              %span.likes
                =fa_icon "heart-o"
                135
      .contents__all-items
        = link_to 'すべての商品を見る', ''

    %section
      %h1.pickupbrand ピックアップブランド
      %h2
        = link_to brand_item_path(49), alt: 'シャネル' do
          = "#{@brand1.brand} 新着アイテム"
      .contents__items
        -@items.where(brand_id: 49).order('id DESC').first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            - item.item_images.first(1).each do |image|
              = image_tag(image.image_url, alt: '商品画像')
              %div
                %div
                  = item.item_title
                %span.price 
                  ¥
                  = item.price
                %span.likes
                  =fa_icon "heart-o"
                  135
      .contents__all-items
        = link_to 'すべての商品をみる'

      %h2
        = link_to brand_item_path(50), alt: 'ルイヴィトン' do
          = "#{@brand2.brand} 新着アイテム"
      .contents__items
        -@items.where(brand_id: 50).order('id DESC').first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            - item.item_images.first(1).each do |image|
              = image_tag(image.image_url, alt: '商品画像')
              %div
                %div
                  = item.item_title
                %span.price 
                  ¥
                  = item.price
                %span.likes
                  =fa_icon "heart-o"
                  135
      .contents__all-items
        = link_to 'すべての商品をみる'

      %h2
        = link_to brand_item_path(52), alt: 'シュプリーム' do
          = "#{@brand3.brand} 新着アイテム"
      .contents__items
        -@items.where(brand_id: 52).order('id DESC').first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            - item.item_images.first(1).each do |image|
              = image_tag(image.image_url, alt: '商品画像')
              %div
                %div
                  = item.item_title
                %span.price 
                  ¥
                  = item.price
                %span.likes
                  =fa_icon "heart-o"
                  135
      .contents__all-items
        = link_to 'すべての商品をみる'
        
      %h2
        = link_to brand_item_path(51), alt: 'ナイキ' do
          = "#{@brand4.brand} 新着アイテム"
      .contents__items
        -@items.where(brand_id: 51).order('id DESC').first(4).each do |item|
          = link_to item_path(item.id), class: 'contents__items-frame' do
            - item.item_images.first(1).each do |image|
              = image_tag(image.image_url, alt: '商品画像')
              %div
                %div
                  = item.item_title
                %span.price 
                  ¥
                  = item.price
                %span.likes
                  =fa_icon "heart-o"
                  135
      .contents__all-items
        = link_to 'すべての商品をみる'

.main__foot-visual 
.sellbutton
  = link_to new_item_path do
    %p 出品
    =fa_icon "camera"
= render 'layouts/footer'
