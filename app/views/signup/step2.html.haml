.single-container
  %header.regi-single-header
    %h1
      = link_to '/' do
        =image_tag "/assets/logo-gray.png", class: "regi-single-header__logo"
    %nav.progress-bar
      %ol.clearfix
        %li
          会員情報
          .progress-status
        %li.active
          電話番号認証
          .progress-status
        %li
          お届け先住所入力
          .progress-status
        %li
          支払い方法
          .progress-status
        %li
          完了
          .progress-status
  .regi-main
    .regi-main__header
      %h2 電話番号の確認
    .regi-main__form
      = form_for @user, url: step3_signup_index_path, method: :get do |f|
        = f.fields_for :user_detail, @user.build_user_detail, url: step3_signup_index_path, method: :get do |c|
          .form-group
            %label 携帯電話の番号
            %br
            = c.telephone_field :phone_number, class: "input-default", placeholder: '携帯電話の番号を入力', required: "true"
            %p 本人確認のため、携帯電話のSMS（ショートメッセージサービス）を利用して認証を行います。

          .form-group
            = f.submit "SMSを送信する", class: 'btn'
            %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。            
          .form-group
            %p.text-right
              = link_to "電話番号の確認が必要な理由＞", "https://www.google.com/"
= render 'layouts/login_footer'




